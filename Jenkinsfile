pipeline {
    agent any

    stages {
        stage("build") {
            steps {
                echo "building..."
            }
        }

        stage("test") {
            steps {
                echo "testing..."
            }
        }

        stage("deploy") {
            steps {
                echo "testing..."
            }
        }
    }

    // execute some logic AFTER all stages executed.
    post {
        always {

        }

        success {

        }

        failure {

        }
    }
}
